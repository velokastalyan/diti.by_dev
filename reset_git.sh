#!/bin/bash
echo "🧨 УДАЛЕНИЕ ИНДЕКСА GIT..."

# 1. Физически удаляем файл индекса (это безопасно для ваших файлов)
rm -f .git/index

# 2. Сбрасываем состояние
git reset

# 3. Добавляем файлы заново 
# (В этот момент Git прочитает .gitignore и ВОЗЬМЕТ ТОЛЬКО КОД)
git add .

# 4. Фиксируем результат
git commit -m "System: Hard reset of Git index"

# 5. Проверяем (должно быть мгновенно)
./checkpoint "Test after reset"
