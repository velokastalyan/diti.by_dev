
version: '3.8'

services:
  web:
    build: .
    ports:
      - "8091:80"
    volumes:
      - .:/var/www/html
    environment:
      - DB_HOST=db
      - DB_USER=sportuser
      - DB_PASSWORD=mZ7oL9sD4g
      - DB_DATABASE=user2160086_timistkas_sportmax
    depends_on:
      - db

  db:
    image: mysql:5.7
    environment:
      MYSQL_DATABASE: user2160086_timistkas_sportmax
      MYSQL_USER: sportuser
      MYSQL_PASSWORD: mZ7oL9sD4g
      MYSQL_ROOT_PASSWORD: rootpassword
    volumes:
      - db_data:/var/lib/mysql
      # Auto-import SQL file
      - ./user2160086_timistkas_sportmax.sql:/docker-entrypoint-initdb.d/dump.sql
    ports:
      - "3307:3306"

volumes:
  db_data:
