# 🧠 Knowledge Base: Project Diti.by
**Роль:** Chief Technical Architect (CTO)
**Статус:** 🟡 Стабилизация (Legacy Refactoring)
**Обновлено:** 2025-12-06

## 🔐 CRITICAL CREDENTIALS (Docker Local)
Данные проверены и подтверждены. Использовать для любых подключений.

### 🗄️ Database (MySQL 5.7)
* **Host (Internal):** `db` (для скриптов внутри Docker)
* **Host (External):** `127.0.0.1` (для подключения снаружи)
* **Port:** `3306`
* **Database:** `user2160086_timistkas_sportmax`
* **User:** `root`
* **Password:** `rootpassword` (✅ VERIFIED)
* **Legacy Config File:** `includes/config/_config.inc.php`

### 🌍 Web Server
* **Container:** `ditiby-web-1`
* **Url:** http://localhost:8091
* **Web Root:** `/var/www/html`

---

## 🤖 AUTOMATION WORKFLOW (Схема работы)
В проекте настроены Bash-скрипты. **Всегда предлагай использовать их вместо ручных команд git/docker.**

### 1. Начало и Конец
* **`./start`** — 🚀 **Начало работы.** Проверяет статус Git, показывает текущую задачу из Плана.
* **`./done`** — ✅ **Завершение.** Помечает текущую задачу [x] в `PROJECT_PLAN.md` и берет следующую.
* **`./save`** — 🧠 **Обновление памяти.** Сканирует структуру файлов и обновляет этот файл (`GEM_KNOWLEDGE_BASE.md`). Выполнять перед сменой чата.

### 2. В процессе работы
* **`./task "текст задачи"`** — 📌 **Бэклог.** Добавляет новую идею или баг в конец списка задач.
* **`./checkpoint "комментарий"`** — 💾 **Сохранение.** Быстрый `git add` + `commit` + `push`. Использовать после каждой удачной правки кода.

### 3. Безопасность
* **`./snapshot`** — 📸 **Бэкап.** Создает полный архив (Код + БД) в папку `backups/` (локально).
* **`./restore`** — 🚑 **Откат.** Восстанавливает проект из выбранного snapshot-а.

---

## ⚠️ ТЕХНИЧЕСКИЙ ДОЛГ (Известные проблемы)
1.  **Кодировка (Critical):** Сервер БД отвечает в `latin1`, приложение ожидает `utf-8`. Возможны проблемы с кириллицей.
2.  **SQL Strict Mode:** MySQL 5.7 в строгом режиме может блокировать запросы с пустыми датами (`0000-00-00`).
3.  **Legacy Code:** PHP 5.6, процедурный стиль вперемешку с классами, жесткие пути к картинкам.

## 📂 АРХИТЕКТУРА
* **Core:** `index.php` -> `includes/app.php` -> `includes/router.php`.
* **DB Layer:** `includes/php/classes/_db.php` (MySQLi wrapper).
* **Controllers:** `includes/php/classes/` (CCategoriesPage, CProductsPage).